<h3 class="link-to-back"><%= link_to 'Voltar', root_path %></h3>

<div class="customers-div">
	<h1>Lista de Clientes</h1>

	<h3><%= link_to 'Cadastrar Cliente', new_customer_path%></li></h3>
	
	<%= render "search_field" %>

	<% if @customers.any? %>
		<%= render "table" %>
	<% else %>
		<p class="msg-error">
			<%= "Nenhum cliente cadastrado" %>
		</p>
	<% end %>
</div>
