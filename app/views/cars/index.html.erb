<h1>Carros Cadastrados</h1>

<%= link_to 'Voltar', root_path %>

<% if @cars.empty? %>
	<%= 'Nenhum carro cadastrado' %>
<% end %>

<% @cars.each do |car| %>
	<h2><%= link_to car.license_plate, car %></h2>
<% end %>
