<% @manufacturer.errors.full_messages.each do |message| %>
	<p><%= message %></p>
<% end %>


<%= form_with(model: @manufacturer) do |f| %>
	<%= f.label :name, 'Nome' %>
	<%= f.text_field :name %>
	<%= f.submit 'Enviar'%>
<% end %>
